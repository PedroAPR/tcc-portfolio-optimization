# =============================================================================
# Dependências do pipeline (TCC) — instalar com:
#     pip install -r requirements.txt
#
# Python >= 3.10 recomendado (idealmente 3.11.9 devido ao TensorFlow).
# Versões indicadas como pisos mínimos (>=) compatíveis com os recursos usados.
# =============================================================================

# --- Núcleo de dados e cálculo numérico ---
pandas>=2.0          # manipulação de séries/painéis e tabelas MultiIndex
numpy>=1.24          # álgebra linear e operações matriciais vetoriais
scipy>=1.10          # otimizações não-convexas e distribuições estatísticas

# --- Otimização convexa e regularização ---
cvxpy>=1.3           # solver de otimização convexa (CVaR, CDaR e restrições CVM 175)

# --- Econometria e modelagem estocástica ---
statsmodels>=0.14    # testes de raiz unitária, CAPM e erros-padrão HAC (Newey-West)
arch>=6.0            # estimação de volatilidade condicional e modelos GARCH


# --- Pré-processamento e utilitários estatísticos ---
scikit-learn>=1.2    # MinMaxScaler para dados de treinamento do LSTM/HMM

# --- Download de dados externos e APIs ---
yfinance>=0.2        # download automatizado de tickers e pesos de mercado
requests>=2.31       # requisições HTTP para API do SGS/Banco Central (CDI/SELIC)
urllib3>=2.0         # controle de conexões de rede e certificados SSL

# --- Entrada/saída de arquivos e persistência ---
pyarrow>=12.0        # gravação e leitura otimizada de tabelas em Parquet
openpyxl>=3.1        # leitura de planilhas Excel estruturadas da Economatica

# --- Visualização de dados e plotagem ---
matplotlib>=3.7      # gráficos de backtest e fronteiras eficientes
seaborn>=0.12        # gráficos descritivos estáticos
plotly>=5.15         # visualizações interativas de dados

# --- Ferramentas CLI, relatórios e progresso ---
tabulate>=0.9        # formatação de relatórios de desempenho em tabelas texto
tqdm>=4.65           # barra de progresso para loops de longa duração (ex: bootstrap)

# --- Ambiente Jupyter ---
jupyterlab>=4.0      # interface de desenvolvimento para os notebooks
ipykernel>=6.0       # kernel Python para execução do código interativo
